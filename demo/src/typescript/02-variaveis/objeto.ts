// variavel object
let produto: object = {
    name: "Fulano",
    cidade: "lx",
    idade: 30
};

// classe
type ProdutoLoja = {
  nome: string;
  preco: number;
  unidades: number;
}

// objeto - deve colocaor todos os atributos
let meuProduto: ProdutoLoja = {
    nome: "Tenis",
    preco: 100.5,
    unidades: 2
}